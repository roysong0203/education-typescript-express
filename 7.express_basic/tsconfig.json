{
  "compilerOptions": {
    // JavaScript로 변환될 때 사용할 ECMAScript 버전 (ES2016은 ES7)
    // 예: async/await 등을 지원하려면 적절한 타겟이 필요
    "target": "es2016",   

    // 모듈 시스템 설정 (Node.js에서는 commonjs 사용)
    // 예: require(), module.exports를 사용하게 됨
    "module": "commonjs",                            

    // TypeScript 소스 코드가 위치한 루트 디렉토리
    // 예: 소스 파일이 ./src에 있다면 컴파일 기준 경로로 사용
    "rootDir": "./src",

    // 컴파일된 JavaScript 파일이 출력될 디렉토리
    // 예: ./dist 폴더에 결과물이 생성됨
    "outDir": "./dist",

    // CommonJS 방식 모듈(import/export)과 ES 모듈 간의 호환성 보장
    // 예: `import fs from 'fs'`처럼 ES 스타일 import가 가능하게 함
    "esModuleInterop": true,       

    // TypeScript의 모든 엄격한 타입 검사 기능을 활성화
    // 예: null 체크, 암시적 any 방지 등 엄격한 코드 작성 유도
    "strict": true,                              
  }
}
